name: docovue
description: Privacy-first document scanning and OCR plugin for Flutter. Auto-capture with liveness detection. Supports Aadhaar, PAN, passports, credit cards, and more with 100% on-device processing.
version: 1.0.0
homepage: https://praveen-dev.space
repository: https://github.com/prawinn04/docovue
issue_tracker: https://github.com/prawinn04/docovue/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  camera: ^0.10.5+5
  collection: ^1.17.2
  flutter:
    sdk: flutter
  meta: ^1.9.1

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.docovue.docovue
        pluginClass: DocovuePlugin
      ios:
        pluginClass: DocovuePlugin

topics:
  - ocr
  - document-scanning
  - auto-capture
  - privacy
  - ml-kit